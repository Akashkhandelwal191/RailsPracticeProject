<%= form_with(model: [current_user, @review], url: product_reviews_path(@product), remote: true) do |form| %>
  <%= form.label :description %>
  <%= form.text_area :description %>

  <%= form.label :rating %>
  <%= form.number_field :rating %>

  <%= form.submit 'Submit Review' %>
<% end %>
